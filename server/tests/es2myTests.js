const expect = require('chai').expect;

describe('es2my tests', () => {

    describe('#elasticsearchResponseToMyResponse', () => {

        it('test 1', () => {
            expect([]).to.be.empty;
        });
    });
});
